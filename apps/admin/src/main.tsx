import React from 'react';
import ReactDOM from 'react-dom/client';

// App shell added in TASK-025

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>Admin app scaffold — TASK-025 will build this out</div>
  </React.StrictMode>,
);
