export enum Channel {
  Email = 'email',
  Sms = 'sms',
  WhatsApp = 'whatsapp',
  Call = 'call',
  WebForm = 'web_form',
  Facebook = 'facebook',
  JustDial = 'justdial',
  Note = 'note',
}

export enum Direction {
  Inbound = 'inbound',
  Outbound = 'outbound',
}
